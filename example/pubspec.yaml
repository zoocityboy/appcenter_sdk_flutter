name: appcenter_example
description: AppCenter testing example app.
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 0.0.1

environment:
  sdk: ">=2.18.5"

dependencies:
  appcenter:
    path: ../
  faker: ^2.1.0
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
